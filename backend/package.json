{
  "devDependencies": {
    "@types/express-pino-logger": "^4.0.3",
    "typescript": "^4.9.4"
  },
  "scripts": {
    "dev-local": "set NODE_ENV=dev && ts-node-dev --respawn --transpile-only src/app.ts",
    "dev": "set NODE_ENV=dev && node build/app.js",
    "start": "set NODE_ENV=prod && node build/app.js",
    "build": "tsc",
    "dev:debug": "nodemon --config nodemon.json src/app.ts"
  },
  "dependencies": {
    "@types/express": "^4.17.15",
    "dayjs": "^1.11.7",
    "express": "^4.18.2",
    "logger": "^0.0.1",
    "pino": "^8.8.0",
    "pino-pretty": "^9.1.1",
    "ts-node-dev": "^2.0.0"
  }
}
